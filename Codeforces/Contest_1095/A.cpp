#include <bits/stdc++.h>
int main() {
    int t, n, result = 0; 
    
    std::cin >> t;
    for (int i = 1; i <= t; i++) {
        std::cin >> n;

        std::vector<int> arr(n);
        for (int i = 0; i < n; i++) {
            std::cin >> arr[i];
        }
            
        
    }
    std::cout << result << std::endl;
    return 0;
}
